# 注册时的应用名称
spring:
  application:
    name: eureka-client
  profiles:
    active: dev1
#    注册地址
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:8761/eureka/
---
spring:
  profiles: dev
server:
  port: 8762
---
spring:
  profiles: dev1
server:
  port: 8763
---
